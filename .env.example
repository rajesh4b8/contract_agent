# General Configuration
ENVIRONMENT=development
# Log verbosity: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# Neo4j connection
NEO4J_URI=your_neo4j_uri
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your_neo4j_password
NEO4J_DATABASE=neo4j
AURA_INSTANCEID=your_aura_instance_id
AURA_INSTANCENAME=your_aura_instance_name

# LLM API keys
OPENAI_API_KEY=
GOOGLE_API_KEY=your_google_api_key
GEMINI_API_KEY=your_google_api_key  # Optional: Fallback for GOOGLE_API_KEY
ANTHROPIC_API_KEY=
MISTRAL_API_KEY=
GROQ_API_KEY=

# Frontend Configuration
VITE_BACKEND_URL=http://localhost:8000

# Tracing and Telemetry
PHOENIX_COLLECTOR_ENDPOINT=http://localhost:6006/v1/traces

# Model Configuration
# Catalogue of selectable models: backend/shared/config/models.py
# Override the provider model string each catalogue entry resolves to:
GEMINI_FLASH_LITE_MODEL=gemini-flash-lite-latest
GEMINI_FLASH_MODEL=gemini-flash-latest
GEMINI_PRO_MODEL=gemini-pro-latest
# Public model id used as the app default / fallback:
DEFAULT_MODEL_ID=gemini-flash

# Redis Cache (Optional for Capstone)
# Redis is not required for the current phase but can be enabled for production performance
REDIS_URL=redis://localhost:6379
CACHE_ENABLED=false
CACHE_DEFAULT_TTL=3600

# Cache TTL by operation (seconds)
CACHE_TTL_DEVIATION=1800
CACHE_TTL_JURISDICTION=3600
CACHE_TTL_PRECEDENT=7200

# Performance Monitoring
MONITORING_ENABLED=true
ALERT_THRESHOLD_CUAD=5000
ALERT_THRESHOLD_DEVIATION=2000
ALERT_THRESHOLD_PRECEDENT=3000
ALERT_THRESHOLD_JURISDICTION=1000

# Parallel Processing
MAX_WORKERS_DEVIATION=3
MAX_WORKERS_PRECEDENT=5
MAX_WORKERS_BATCH=5

# Timeouts (seconds)
TIMEOUT_PATTERN_MATCHING=1.0
TIMEOUT_PRECEDENT_CLAUSE=5.0
TIMEOUT_BATCH_CONTRACT=30.0

# Batch Processing
MAX_BATCH_SIZE=50
BACKGROUND_BATCH_THRESHOLD=10
BATCH_SEMAPHORE_LIMIT=5

# Feature Flags
ENABLE_SEMANTIC_ANALYSIS=true
ENABLE_PARALLEL_PROCESSING=true
ENABLE_ADAPTIVE_LEARNING=true
ENABLE_PHASE_FALLBACK=true

# Mock auth for local development. There is no real authentication yet: when a
# request carries no X-User-Role header, production denies beyond VIEWER while
# development falls back to this role so the UI (which sends no header) works.
# One of: ADMIN | LEGAL_REVIEWER | AUDITOR | VIEWER
DEV_DEFAULT_ROLE=LEGAL_REVIEWER
